# Agent Notes

- When changing persisted configuration schemas, do not add backward compatibility for old configuration formats unless explicitly requested.
- When Windows and macOS implementations differ substantially, keep them in separate platform-specific files instead of one large conditional-compilation block.
